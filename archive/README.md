# Archived analysis snapshot

`estimators_rr_v7_FROZEN.R` is the byte-identical frozen estimator file from the final analysis lineage. Its MD5 checksum is:

```text
30476166f4b698161bc2304cef98e2c0
```

The public file `R/02_estimators.R` contains the same executable R statements with development-oriented comments cleaned for readability.

The internal final-results archive recorded the DGP checksum separately. The public DGP in `R/01_data_generating_mechanism.R` is intentionally a cleaned extraction of the active generator and diagnostics rather than a byte-level archive copy.
